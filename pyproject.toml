[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "colmap_cameras"
version = "1.1.0"

[tool.setuptools]
packages = ["colmap_cameras", "colmap_cameras.models", "colmap_cameras.utils", "colmap_cameras.adapters", "colmap_cameras.apps"]
